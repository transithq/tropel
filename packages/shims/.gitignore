# Generated by scripts/build.sh — refreshed from ../../js/ (source of truth).
dist/
shim/
node_modules/

# CI's `npm install` generates this; npm strips it from the published
# tarball automatically, so it's pure drift noise in the monorepo.
package-lock.json

# Manual `npm pack` output.
*.tgz
